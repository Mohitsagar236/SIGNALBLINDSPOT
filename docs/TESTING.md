# Testing

Vitest covers scoring, CSV validation, blind-spot detection, recommendations, reports, and integration flows. Playwright covers the primary demo flow from dashboard to roadmap detail to report export.

Run:

- `npm.cmd run test`
- `npm.cmd run test:e2e`
